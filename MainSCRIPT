
if _G.MovesetSettings.DoubleSwitch then

  local SkillName = "Divine Pummel"
local CooldownLengh = 2.5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Normal Punch"],
        ["Goal"] = "Console Move"
    }
}))
task.wait(0.535)
local RunService = game:GetService("RunService")

local player = game:GetService("Players").LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

local liveFolder = workspace:WaitForChild("Live")

-- 🔍 Find closest target
local function getClosestTarget()
    local closest = nil
    local shortest = math.huge
    
    for _, model in ipairs(liveFolder:GetChildren()) do
        if model:IsA("Model") and model ~= character then
            local targetHRP = model:FindFirstChild("HumanoidRootPart")
            local targetHumanoid = model:FindFirstChildOfClass("Humanoid")

            if targetHRP and targetHumanoid and targetHumanoid.Health > 0 then
                local dist = (targetHRP.Position - hrp.Position).Magnitude
                
                if dist < shortest then
                    closest = model
                    shortest = dist
                end
            end
        end
    end

    return closest
end

local target = getClosestTarget()
if not target then
    warn("No valid target found")
    return
end

local targetHRP = target:FindFirstChild("HumanoidRootPart")

-- save original state
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- tilt (optional)
local faceDown = CFrame.Angles(math.rad(-90), 0, 0)

-- 🚀 TELEPORT ONCE
local lockPosition = targetHRP.Position + Vector3.new(0, 2, 1.25)
hrp.CFrame = CFrame.new(lockPosition) * faceDown

-- 🔒 LOCK POSITION
local connection
connection = RunService.RenderStepped:Connect(function()
    hrp.CFrame = CFrame.new(lockPosition) * faceDown
    hrp.AssemblyAngularVelocity = Vector3.zero
    hrp.AssemblyLinearVelocity = Vector3.zero
end)

task.wait(0.35)

-- restore
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame

end

-- [[ THE CODE, MODIFY IF YOU WANT. ]]
local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "X"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "X"
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(106, 105, 106)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(31,31,31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["X"] = Enum.KeyCode.X,
}
local km = Kmg[ks]

--// INPUT SCRIPT (FIXED)
local player = game:GetService("Players").LocalPlayer
local uis = game:GetService("UserInputService")

-- Force this skill to be key 5
local KeyCode = km

-- Input handling (FIXED)
uis.InputBegan:Connect(function(input, gameProcessed)
	-- Prevent activation while typing/chatting
	if gameProcessed then return end
	
	if input.KeyCode == KeyCode then
		if not OnCooldown then
			cooldownFunction()
			Activated()
		end
	end
end)

local SkillName = "Celestial Ascent"
local CooldownLengh = 2.5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Normal Punch"],
        ["Goal"] = "Console Move"
    }
}))
task.wait(0.35)
local RunService = game:GetService("RunService")

local player = game:GetService("Players").LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

local liveFolder = workspace:WaitForChild("Live")

-- 🔍 Find closest target
local function getClosestTarget()
    local closest = nil
    local shortest = math.huge
    
    for _, model in ipairs(liveFolder:GetChildren()) do
        if model:IsA("Model") and model ~= character then
            local targetHRP = model:FindFirstChild("HumanoidRootPart")
            local targetHumanoid = model:FindFirstChildOfClass("Humanoid")

            if targetHRP and targetHumanoid and targetHumanoid.Health > 0 then
                local dist = (targetHRP.Position - hrp.Position).Magnitude
                
                if dist < shortest then
                    closest = model
                    shortest = dist
                end
            end
        end
    end

    return closest
end

local target = getClosestTarget()
if not target then
    warn("No valid target found")
    return
end

local targetHRP = target:FindFirstChild("HumanoidRootPart")

-- save original state
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- tilt (optional)
local faceDown = CFrame.Angles(math.rad(90), 0, 0)

-- 🚀 TELEPORT ONCE
local lockPosition = targetHRP.Position + Vector3.new(0, -3.5, 0.5)
hrp.CFrame = CFrame.new(lockPosition) * faceDown

-- 🔒 LOCK POSITION
local connection
connection = RunService.RenderStepped:Connect(function()
    hrp.CFrame = CFrame.new(lockPosition) * faceDown
    hrp.AssemblyAngularVelocity = Vector3.zero
    hrp.AssemblyLinearVelocity = Vector3.zero
end)

task.wait(0.75)

-- restore
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame

end

-- [[ THE CODE, MODIFY IF YOU WANT. ]]
local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "C"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "C"
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(106, 105, 106)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(31,31,31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["C"] = Enum.KeyCode.C,
}
local knn = Kmg[ks]

local player = game:GetService("Players").LocalPlayer
local uis = game:GetService("UserInputService")

-- Force this skill to be key 5
local KeyCode = knn

-- Input handling (FIXED)
uis.InputBegan:Connect(function(input, gameProcessed)
	-- Prevent activation while typing/chatting
	if gameProcessed then return end
	
	if input.KeyCode == KeyCode then
		if not OnCooldown then
			cooldownFunction()
			Activated()
		end
	end
end)

game.Players.LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar["5"].LayoutOrder = 0
wait(0.125)


else

  local SkillName = "Head Swipe"
local CooldownLengh = 2.5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Normal Punch"],
        ["Goal"] = "Console Move"
    }
}))
task.wait(0.35)
local RunService = game:GetService("RunService")

local player = game:GetService("Players").LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

local liveFolder = workspace:WaitForChild("Live")

local function getClosestTarget()
    local closest = nil
    local shortest = math.huge
    
    for _, model in ipairs(liveFolder:GetChildren()) do
        if model:IsA("Model") and model ~= character then
            local targetHRP = model:FindFirstChild("HumanoidRootPart")
            local targetHumanoid = model:FindFirstChildOfClass("Humanoid")

            if targetHRP and targetHumanoid and targetHumanoid.Health > 0 then
                local dist = (targetHRP.Position - hrp.Position).Magnitude
                
                if dist < shortest then
                    closest = model
                    shortest = dist
                end
            end
        end
    end

    return closest
end

local target = getClosestTarget()
if not target then
    warn("No valid target found")
    return
end

local targetHRP = target:FindFirstChild("HumanoidRootPart")

local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

local faceDown = CFrame.Angles(math.rad(-180), 0, 0)


local lockPosition = targetHRP.Position + Vector3.new(0, 0, -5)
hrp.CFrame = CFrame.new(lockPosition) * faceDown


local connection
connection = RunService.RenderStepped:Connect(function()
    hrp.CFrame = CFrame.new(lockPosition) * faceDown
    hrp.AssemblyAngularVelocity = Vector3.zero
    hrp.AssemblyLinearVelocity = Vector3.zero
end)

task.wait(0.75)


connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame

end


local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "_G.MovesetSettings.SubmovesKeybindChanger.S1"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "_G.MovesetSettings.SubmovesKeybindChanger.S1"
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(106, 105, 106)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(31,31,31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["X"] = Enum.KeyCode.X,
}
local km = Kmg[ks]

--// INPUT SCRIPT (FIXED)
local player = game:GetService("Players").LocalPlayer
local uis = game:GetService("UserInputService")

-- Force this skill to be key 5
local KeyCode = km

-- Input handling (FIXED)
uis.InputBegan:Connect(function(input, gameProcessed)
	-- Prevent activation while typing/chatting
	if gameProcessed then return end
	
	if input.KeyCode == KeyCode then
		if not OnCooldown then
			cooldownFunction()
			Activated()
		end
	end
end)
      


local SkillName = "Martial Swipe"
local CooldownLengh = 2.5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()

game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Shove"],
        ["Goal"] = "Console Move"
    }
}))


wait(0.45)
local animation = Instance.new("Animation")
animation.AnimationId = "rbxassetid://136370737633649"
        
        local chr = game.Players.LocalPlayer.Character
        local humanoid = chr:WaitForChild("Humanoid")
        
        local animTrack = humanoid:LoadAnimation(animation)
        animTrack:Play()
animTrack:AdjustSpeed(2.5)
animTrack.TimePosition = 2.25
        task.delay(0.35, 

function() 

animTrack:Stop()


end)
task.wait(0.35)
local RunService = game:GetService("RunService")

local player = game:GetService("Players").LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

local liveFolder = workspace:WaitForChild("Live")

-- Function: find closest valid target (NOT yourself)
local function getClosestTarget()
    local closest = nil
    local shortest = math.huge
    
    for _, model in ipairs(liveFolder:GetChildren()) do
        if model:IsA("Model") and model ~= character then -- 🚫 ignore yourself
            
            local targetHRP = model:FindFirstChild("HumanoidRootPart")
            local targetHumanoid = model:FindFirstChildOfClass("Humanoid")

            if targetHRP and targetHumanoid and targetHumanoid.Health > 0 then
                local dist = (targetHRP.Position - hrp.Position).Magnitude
                
                if dist < shortest then
                    closest = model
                    shortest = dist
                end
            end
        end
    end

    return closest
end

-- Begin action -------------------------------------------------------

local target = getClosestTarget()
if not target then
    warn("No valid target found")
    return
end

local targetHRP = target:FindFirstChild("HumanoidRootPart")

-- Store original states
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

-- Disable movement
humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- Face down
local faceDown = CFrame.Angles(math.rad(-180), 0, 0)

-- Lock position for 1 second
local connection
connection = RunService.RenderStepped:Connect(function()
    if targetHRP and targetHRP.Parent then
        local lockPos = targetHRP.Position + Vector3.new(0, 1.5, -4.5)
        hrp.CFrame = CFrame.new(lockPos) * faceDown
        hrp.AssemblyAngularVelocity = Vector3.zero
    end
end)

-- Fire tool
character:FindFirstChild("Communicate"):FireServer(unpack({
    [1] = {
        ["Tool"] = player.Backpack:FindFirstChild("Normal Punch"),
        ["Goal"] = "Console Move"
    }
}))

task.wait(1.2)

-- Restore
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame

end

-- [[ THE CODE, MODIFY IF YOU WANT. ]]
local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "C"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "C"
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(106, 105, 106)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(31,31,31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["C"] = Enum.KeyCode.C
}
local kb = Kmg[ks]

local player = game:GetService("Players").LocalPlayer
local uis = game:GetService("UserInputService")

-- Force this skill to be key 5
local KeyCode = kb

-- Input handling (FIXED)
uis.InputBegan:Connect(function(input, gameProcessed)
	-- Prevent activation while typing/chatting
	if gameProcessed then return end
	
	if input.KeyCode == KeyCode then
		if not OnCooldown then
			cooldownFunction()
			Activated()
		end
	end
end)

game.Players.LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar["5"].LayoutOrder = 0
wait(0.125)

end

game.Players.LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar["6"].Base.Number.Text = "C"
game.Players.LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar["6"].Base.Number.Number.Text = "C"
game.Players.LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar["5"].Base.Number.Text = "X"
game.Players.LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar["5"].Base.Number.Number.Text = "X"
wait(0.65)
local player = game.Players.LocalPlayer
local hotbar = player:WaitForChild("PlayerGui"):WaitForChild("Hotbar")
	:WaitForChild("Backpack"):WaitForChild("Hotbar")

local slot5 = hotbar:FindFirstChild("5")
local slot6 = hotbar:FindFirstChild("6")

if slot5 and slot5:FindFirstChild("Base") then
	slot5.Base.Position = UDim2.new(0.35, 0, 0.5, 0)
end

if slot6 and slot6:FindFirstChild("Base") then
	slot6.Base.Position = UDim2.new(0.675, 0, 0.5, 0)
end

local settings = _G.MovesetSettings.UltName
game.Players.LocalPlayer.PlayerGui.Bar.MagicHealth.TextLabel.Text = settings
game.Players.LocalPlayer.PlayerGui.Bar.MagicHealth.TextLabel.TextLabel.Text = settings


local moves = _G.MovesetSettings.MoveNames

local player = game.Players.LocalPlayer
	local char = player.Character
	local Humanoid = char.Humanoid
	local hot = player.PlayerGui:WaitForChild("Hotbar")
	local hotbar = hot:WaitForChild("Backpack"):WaitForChild("Hotbar")

	local function cloneToolName(slot, text)
		local toolName = slot.ToolName
		if not slot:FindFirstChild("CustomName") then
			toolName.Visible = false
			local clone = toolName:Clone()
			clone.Name = "CustomName"
			clone.Parent = slot
			clone.Text = text
			clone.Visible = true
		elseif slot:FindFirstChild("CustomName") then
			if slot:FindFirstChild("CustomName").Text ~= text then
				toolName.Visible = false
				slot:FindFirstChild("CustomName").Text = text
			end
		end
	end


	local toolNameMap = {
		["Normal Punch"] = moves.NormP,
		["Consecutive Punches"] = moves.Cons,
		["Shove"] = moves.Shove,
		["Uppercut"] = moves.Upper
		
	}

	local function typeText(label, text, delay)
		delay = delay or 0
		local originalText = text
		label.Text = ""
		for i = 1, #originalText do
			label.Text = string.sub(originalText, 0.1, i)
			task.wait(delay)
		end
	end



	local typedTools = {}

	local function updateToolNames()
		for _, slot in ipairs(hotbar:GetChildren()) do
			if slot:FindFirstChild("Base") and slot.Base:FindFirstChild("ToolName") then
				local toolNameText = slot.Base.ToolName.Text
				local newName = toolNameMap[toolNameText]
				if newName then
					cloneToolName(slot.Base, newName)
					if slot.Base:FindFirstChild("CustomName") and not typedTools[newName] then
						typeText(slot.Base.CustomName, newName, 0.0)
						typedTools[newName] = true


					end
				end
			end
		end
	end

	task.spawn(
		function()
			local currentUltIndex = 1

			while true do
				typeText(UltLabel, ultTexts[currentUltIndex], 0.000)
				currentUltIndex = (currentUltIndex % #ultTexts) + 1

				pcall(
					function()
					end
				)

				updateToolNames()
				task.wait(0.1)
			end
		end
	)

	task.spawn(
		function()
			while task.wait(0.01) do
				updateToolNames()
			end
		end)

if _G.MovesetSettings.NoWalls then


local tuff = "Part"
local newColor =  Color3.new(0.65,0.85,1)

for _, descendant in pairs(game.Workspace.Map.Walls:GetDescendants()) do
    if descendant:IsA("Part") and descendant.Name == tuff then
        -- Change the color to a solid color
        descendant.Transparency = 1
        descendant.CanCollide = false
    end
end

local tuff1 = "Part"
local newColor =  Color3.new(0.65,0.85,1)

for _, descendant in pairs(game.Workspace.Map.GrassTop:GetDescendants()) do
    if descendant:IsA("Part") and descendant.Name == tuff1 then
        -- Change the color to a solid color
        descendant.Transparency = 1
        descendant.CanCollide = false
    end
end
else


local tuff = "Part"
local newColor =  Color3.new(0.65,0.85,1)

for _, descendant in pairs(game.Workspace.Map.Walls:GetDescendants()) do
    if descendant:IsA("Part") and descendant.Name == tuff then
        -- Change the color to a solid color
        descendant.Transparency = 0
        descendant.CanCollide = true
    end
end

local tuff1 = "Part"
local newColor =  Color3.new(0.65,0.85,1)

for _, descendant in pairs(game.Workspace.Map.GrassTop:GetDescendants()) do
    if descendant:IsA("Part") and descendant.Name == tuff1 then
        -- Change the color to a solid color
        descendant.Transparency = 0.00000001
        descendant.CanCollide = true
    end
end
end


local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local p = game.Players.LocalPlayer
local Humanoid = p.Character:WaitForChild("Humanoid")
local AnimAnim = Instance.new("Animation")

--[[Animations]]

--[[Move 1]]

local animationId = 10468665991

local function onAnimationPlayed(animationTrack)
    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do
    animTrack:Stop()

end

AnimAnim.AnimationId = "rbxassetid://76530443909428"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 1.35

Anim:AdjustSpeed(-1)
task.delay(0.58, function()
Anim:Stop()
end) wait(0.565)
AnimAnim.AnimationId = "rbxassetid://79761806706382"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 3

Anim:AdjustSpeed(1)

    end

end

--[[END OF MOVE 1 ANIM]]

--[[Move 2]]


humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local animationId = 10466974800

local function onAnimationPlayed(animationTrack)
    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end

AnimAnim.AnimationId = "rbxassetid://125955606488863"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 0

Anim:AdjustSpeed(1.5)

task.delay(0.225, function()
Anim:Stop()
end)
wait(0.221)
AnimAnim.AnimationId = "rbxassetid://18897695481"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 0

Anim:AdjustSpeed(2.5)

task.delay(1.35, function()
Anim:Stop()
end)
wait(1.35)
AnimAnim.AnimationId = "rbxassetid://14900168720"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 2

Anim:AdjustSpeed(2)
    end

end

--[[END OF MOVE 2 ANIM]]

--[[Move 3]]


humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local animationId = 10471336737

local function onAnimationPlayed(animationTrack)
    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end

AnimAnim.AnimationId = "rbxassetid://135289891173395"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 0
Anim:AdjustSpeed(0.25)
wait(0.125)

Anim:AdjustSpeed(0.875)


delay(0.565, function()

    Anim:Stop()

end)
wait(0.525)
AnimAnim.AnimationId = "rbxassetid://109617620932970"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 0.65

Anim:AdjustSpeed(1.75)

    end

end

--[[END OF MOVE 3 ANIM]]

--[[Move 4]]


humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local animationId = 12510170988

local function onAnimationPlayed(animationTrack)
    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end

AnimAnim.AnimationId = "rbxassetid://18179181663"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 0

Anim:AdjustSpeed(1)


    end

end

--[[END OF MOVE 4 ANIM]]

--[[Wall combo]]

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 15955393872

local function onAnimationPlayed(animationTrack)
    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end

AnimAnim.AnimationId = "rbxassetid://15943915877"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim:AdjustSpeed(1)

Anim.TimePosition = 0


    end

end

--[[END OF WALL COMBO ANIM]]

--[[Ult Activation]]

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 12447707844

local function onAnimationPlayed(animationTrack)
    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end

AnimAnim.AnimationId = "rbxassetid://17106858586"

local Anim = Humanoid:LoadAnimation(AnimAnim)

Anim:Play()

Anim.TimePosition = 1

Anim:AdjustSpeed(0)

    end

end

--[[END OF ULT ACTIVATION ANIM]]

--[[Punch anims]]

humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local Players = game:GetService("Players")

local player = Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local animationIdsToStop = {

    [10469493270] = true, --punch1

    [10469630950] = true, --punch2

    [10469639222] = true, --punch3

    [10469643643] = true, --punch4

}


local replacementAnimations = {

    ["10469493270"] = "rbxassetid://17889458563", --punch1

    ["10469630950"] = "rbxassetid://17889290569", --punch2

    ["10469639222"] = "rbxassetid://16944265635", --punch3

    ["10469643643"] = "rbxassetid://13294471966", --punch4

}


local queue = {}

local isAnimating = false


local function playReplacementAnimation(animationId)

    if isAnimating then

        table.insert(queue, animationId)

        return

    end

   

    isAnimating = true

    local replacementAnimationId = replacementAnimations[tostring(animationId)]

    if replacementAnimationId then

        local AnimAnim = Instance.new("Animation")

        AnimAnim.AnimationId = replacementAnimationId

        local Anim = humanoid:LoadAnimation(AnimAnim)

        Anim:Play()
       Anim:AdjustSpeed(1.35)

        Anim.Stopped:Connect(function()

            isAnimating = false

            if #queue > 0 then

                local nextAnimationId = table.remove(queue, 1)

                playReplacementAnimation(nextAnimationId)

            end

        end)

    else

        isAnimating = false

    end

end


local function stopSpecificAnimations()

    for _, track in ipairs(humanoid:GetPlayingAnimationTracks()) do

        local animationId = tonumber(track.Animation.AnimationId:match("%d+"))

        if animationIdsToStop[animationId] then

            track:Stop()

        end

    end

end


local function onAnimationPlayed(animationTrack)

    local animationId = tonumber(animationTrack.Animation.AnimationId:match("%d+"))

    if animationIdsToStop[animationId] then

        stopSpecificAnimations()

        animationTrack:Stop()
       

        local replacementAnimationId = replacementAnimations[tostring(animationId)]

        if replacementAnimationId then

            playReplacementAnimation(animationId)

        end

    end

end


humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoidRootPart = character:WaitForChild("HumanoidRootPart")


local function onBodyVelocityAdded(bodyVelocity)

    if bodyVelocity:IsA("BodyVelocity") then

        bodyVelocity.Velocity = Vector3.new(bodyVelocity.Velocity.X, 0, bodyVelocity.Velocity.Z)

    end

end


character.DescendantAdded:Connect(onBodyVelocityAdded)


for _, descendant in pairs(character:GetDescendants()) do

    onBodyVelocityAdded(descendant)

end


player.CharacterAdded:Connect(function(newCharacter)

    character = newCharacter

    humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    character.DescendantAdded:Connect(onBodyVelocityAdded)

   

    for _, descendant in pairs(character:GetDescendants()) do

        onBodyVelocityAdded(descendant)

    end

end) 
